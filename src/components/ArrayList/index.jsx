import Array from './Array';

const ArrayList = () => (
  <section className="ArrayList w-full outline">
    <Array />
    <Array />
    <Array />
    <Array />
    <Array />
  </section>
);

export default ArrayList;
